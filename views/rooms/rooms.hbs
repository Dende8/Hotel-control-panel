<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
</head>
<body>
    <h1>
        {{ title }}
    </h1>

    <a href="/auth/login-done">Volver</a>
    <a href="/rooms/create">Registrar habitación</a>
    <ul>
        {{#each rooms}}
        <li>
            <h4>
                <a href="/rooms/{{_id}}">Habitación {{ number }}</a>
            </h4>
            <h4>Tipo: {{ type }}</h4>
            <h4>Está ocupada: {{ isOccupied }}</h4>
            <h4>Huéspedes: </h4>
            {{#each guests}}
                <p>{{this.firstname}} {{this.lastname}}, </p>
            {{/each}}
        </li>
        <hr>
        {{/each}}
    </ul>
</body>
</html>